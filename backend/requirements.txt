# Minimum versions verified on Python 3.14 (prebuilt wheels available).
# Pinned as >= so pip resolves 3.14-compatible wheels instead of building
# old pandas/numpy from source.
fastapi>=0.135.0
uvicorn[standard]>=0.44.0
pydantic>=2.12.0
sqlmodel>=0.0.39
pandas>=2.3.3
numpy>=2.4.0
httpx>=0.28.1
Pillow>=12.0.0
# Loads a local backend/.env for dev secrets (e.g. GEMINI_API_KEY). Optional at
# runtime — main.py imports it defensively — but pinned so `pip install` provides it.
python-dotenv>=1.0.1
pytest>=8.3.0
