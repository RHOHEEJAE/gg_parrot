# Python
__pycache__/
*.pyc
.venv/
venv/
backend/app.db
backend/cache/
.pytest_cache/

# Secrets — API keys go in env vars / a local .env (NEVER commit real keys).
.env
.env.*
!.env.example

# Node / Vite
node_modules/
frontend/dist/
*.local

# 매크로 실행기 빌드 산출물 (exe 는 GitHub Releases 로 배포; 소스만 커밋)
runner/build/
runner/dist/
runner/*.spec

# OS / editor
.DS_Store
.vscode/
.idea/
