# GGparrot 디자인 시스템

껄무새(GGparrot)의 색·조판·컴포넌트 규격. 세 개의 공개 디자인 명세를 역할별로 나눠 조합했고,
벗어난 지점은 전부 이유와 함께 아래에 적어둔다.

| 출처 | 가져온 것 |
|---|---|
| **binance** | 다크 표면(3단) · 단일 액센트 노랑 · 등락 색 · 숫자와 본문 서체 분리 |
| **toss** | 컴포넌트 규격 · 조판 램프 · 간격 · 라운드 사다리 · 모션 · 카피 어조 · 라이트 중성색 |
| **spacex** | 카드·컨테이너를 쓰지 않는 구성 |

명세 원본은 `~/.claude/skills/{binance,toss,kraken,...}/DESIGN.md`.

---

## 1. 원칙

1. **작을수록 대비를 높인다.** 다크에서는 작은 글씨일수록 밝게, 라이트에서는 어둡게. 같은 규칙의 양면이다.
   어두운 바닥에서 가는 획이 번져 보이는 현상(halation)을 보정한다.
2. **제목과 본문은 3px + 무게 200 이상 벌린다.** 자간과 행간도 같이 가른다. 한 축만 바꾸면 위계가 안 선다.
3. **영역을 감싸는 큰 상자는 쓰지 않는다.** 표·배너는 캔버스 위에 괘선으로만 구분한다.
   반면 **손으로 누르는 작은 것(버튼·뱃지·칩)은 채움을 유지한다** — 눌리는 것처럼 보여야 한다.
4. **노란 채움은 화면당 하나.** 가장 중요한 행동 하나에만. 나머지 강조는 명도와 무게로.
5. **숫자는 전용 서체로.** 본문은 Pretendard, 수치는 JetBrains Mono + `tabular-nums`.
6. **유리(backdrop blur)는 무언가가 실제로 무언가를 덮을 때만.** 정지한 카드에는 쓰지 않는다.

---

## 2. 색

### 다크 (기본)

| 역할 | 값 | 대비 | 비고 |
|---|---|---|---|
| 캔버스 | `#0b0e11` | — | binance `canvas-dark` |
| 면(작은 버튼·입력) | `#1e2329` | — | binance `surface-card-dark` |
| 한 단계 위 | `#2b3139` | — | binance `surface-elevated-dark` |
| 괘선 | `#2b3139` | — | binance `hairline-on-dark` — 면과 같은 색 |
| 강조 텍스트 | `#eaecef` | 16.4:1 | 제목 · 이름 · 수치 |
| **작은 글씨** | `#c9cfd8` | 12.3:1 | 13–15px 보조 설명 · 라벨 · 단위 |
| 본문 | `#b7bec9` | 10.3:1 | 17px 문단 |
| 장식 | `#9aa4b2` | 7.7:1 | 화살표 · 구분자. **읽을 필요 없는 것만** |
| 상승 | `#0ecb81` | — | 글자 색으로만. 채움 금지 |
| 하락 | `#f6465d` | — | 글자 색으로만. 채움 금지 |

`#707a8a`(binance `muted`)는 `#0b0e11` 위에서 **4.1:1로 AA 미달**이라 쓰지 않는다.
`#929aa5`(6.8:1)도 AA는 통과하지만 실제로 안 읽혀 퇴출했다.

### 라이트

토스 중성색. 명세가 OKLCH로만 표기해 hex로 변환했다. 순수 회색이 아니라 **차가운 navy 기운**이 돌아
흰 화면이 누렇게 뜨지 않는다 — 노란 액센트와 같이 쓸 때 특히 중요하다.

| 역할 | 값 | 토스 토큰 | 대비 |
|---|---|---|---|
| 캔버스 | `#ffffff` | white | — |
| 페이지 바닥 | `#f6f8fa` | grey-50 | — |
| 면 | `#eef1f4` | grey-100 | — |
| 괘선 | `#dee3e7` | grey-200 | — |
| 강조 텍스트 | `#141f2c` | grey-900 | 16.6:1 |
| **작은 글씨** | `#2d3a48` | grey-800 | 11.6:1 |
| 본문 | `#4b5765` | grey-700 | 7.4:1 |
| 장식 | `#6a7480` | grey-600 | 4.7:1 |
| 상승 | `#007738` | green-500 | 5.7:1 |
| 하락 | `#c81e33` | (조정) | 5.7:1 |

토스 `red-500`(`#f03848`)은 흰 배경에서 3.9:1로 미달이라 `#c81e33`으로 진하게 조정하고
**상승과 명도를 5.7:1로 맞췄다** — 한쪽이 더 튀면 손익이 편향돼 보인다.

### 브랜드 · 포인트

| 역할 | 값 | 규칙 |
|---|---|---|
| 브랜드(CTA) | `#fcd535` | binance 옐로 |
| 그 위 글자 | `#181a20` | 노랑 위엔 항상 이 색 (12.2:1) |
| 호버 | `#ffe066` | |
| 포인트(🪙) | 인디고 계열 | 노랑이 CTA를 맡으므로 앰버에서 이전 |

> **라이트에서 노랑은 채움 전용.** 흰 배경 위 노란 글자는 **1.4:1**로 보이지 않는다.
> 다크에서 노랑이 하던 일(순위 숫자 · 뱃지 글자 · 강조 수치)은 라이트에서 채움과 명도가 대신한다.
> 입력 포커스 테두리도 라이트에서는 `#c9a300`으로 내린다.

---

## 3. 조판

### 서체

| 용도 | 서체 | 근거 |
|---|---|---|
| 본문 · UI | **Pretendard Variable** (45–930) | 토스가 TPS 대체로 공식 권장 — 메트릭·한글 자형·tabular figure가 거의 1:1 |
| 숫자 · 수치 | **JetBrains Mono Variable** | binance가 BinancePlex 대체로 지정. OFL 라이선스 |

`frontend/src/assets/fonts/` 에 서브셋으로 적재. 앱이 실제로 쓰는 한글 695자 + 라틴을
`Pretendard-0.woff2`(157KB)에 담아 항상 로드하고, 나머지 한글 완성형은 8개 조각으로 나눠
`unicode-range`로 필요할 때만 받는다.

### 램프 (토스 값 · 다크 보정 포함)

| 토큰 | 크기 / 행간 / 자간 / 무게 | 쓰는 곳 |
|---|---|---|
| `h2` | 24 / 1.3 / −0.02em / 700 | 화면 제목 |
| `h4` | 20 / 1.35 / −0.015em / 700 | 섹션 제목 |
| `title` | 17 / 1.3 / −0.015em / 700 | **행 제목 · 카드 제목** |
| `body` | 17 / 1.55 / −0.005em / 400 | 문단 |
| `label` | 15 / 1.25 / −0.005em / 600 | 표 라벨 · 버튼 M |
| `small` | 14 / 1.5 / −0.005em / **500** | 보조 설명 · 도움말 |
| `caption` | 13 / 1.4 / 0 / **600** | 단위 · 캡션 |

토스 원본은 `body-3` 13/400, `caption` 12/500이지만 **다크에서 안 읽혀 한 단계씩 올렸다.**
비율 관계는 유지했으므로 위계는 그대로다. **13px 이하에 weight 400을 쓰지 않는다.**

제목과 본문의 거리는 `title`(17/700/−0.015em/1.3) ↔ `small`(14/500/−0.005em/1.5) 이 기준이다 —
크기 3px, 무게 200, 자간·행간까지 네 축이 모두 갈린다.

---

## 4. 형태

**라운드** (토스 사다리, 버튼은 크기와 짝)

```
4 · 8 · 12 · 14 · 16 · 20 · 24 · 999
버튼  XL 56→16   L 48→14   M 40→12   S 32→10
```

**간격** — 4의 배수. 화면 바깥 여백 24, 목록 행 간 16, 라벨과 입력 사이 8.

**깊이** — 평면이 기본. 그림자는 떠 있는 것(모달·토스트)에만. 눌림 상태는 그림자가 아니라
검정 26% 오버레이. 비활성은 노드 전체 `opacity: 0.30`.

**모션** — 120 / 200 / 320ms 세 단계, `cubic-bezier(.22,.61,.36,1)`.
바운스 오버슈트 · parallax · skeleton shimmer 없음.

---

## 5. 컴포넌트

토스 규격을 그대로 따른다.

- **`board-row`** (리더보드) — 순위 숫자 + 36px 원형 아바타 + 제목/설명 스택 + 우측 tabular 점수.
  순위 숫자는 브랜드색, **1위만 강조 텍스트색으로 뒤집는다.** 1위 아바타는 노랑으로 채운다.
- **`table-row`** (지표) — 라벨 / 값 / 증감 3열, 1px 괘선으로 구분. 수치는 tabular, 등락은 시맨틱 색.
- **`bar-chart`** — 막대는 회색 기본 + 강조 막대만 노랑. 상단만 6px 라운드.
  **축선·구분선을 두지 않는다.**
- **`text-field`** — 48px, radius 12, 면 배경. 포커스 시 캔버스 배경 + 1.5px 브랜드 테두리.
- **`bottom-cta`** — 56px 고정 액션. 위쪽에 28px 캔버스→투명 보호 그라디언트.
- **`segmented-control`** — 트랙은 면 배경, 선택된 분절만 떠오른다.
- **`chip`** — 34px full pill. **`badge`** — 22px, radius 6, 옅은 배경 + 시맨틱 글자.
- **`progress-stepper`** — 26px 원형 노드. 현재 노드에 4px 글로우 링.
- **`dialog`** — radius 20, 24px 안쪽 여백. **CTA를 가로가 아니라 세로로 쌓는다**(각 48px).

### 유리를 쓰는 곳

| 자리 | 유리 | 값 |
|---|---|---|
| 스티키 상단바 | ✅ | `rgba(11,14,17,.68)` + `saturate(180%) blur(20px)` |
| 하단 고정 시세 띠 | ✅ | 동일 |
| 모달 배경 | ✅ | `rgba(0,0,0,.56)` + `blur(6px)` |
| 표 · 카드 · 배너 | ❌ | 아무것도 덮지 않음 |

---

## 6. 카피

토스 어조를 따른다 — **해요체 + 위임형 + 일상어**.

- 격식체(~합니다) · 단정형(~다) · 챗봇 톤("~해보세요!") · 마케팅 과장을 쓰지 않는다.
- **버튼은 일어날 일을 직접 말한다** — `돌려보기`, `열기`. `여기를 눌러주세요` 금지.
- 에러는 멈춰 세우지 않고 다음 화면으로 안내한다.
- 금액은 `1,000원`처럼 세 자리 구분 + 단위 붙여쓰기.

---

## 7. 명세를 벗어난 지점

의도적으로 어긴 것들. 되돌릴 때 참고.

| 벗어난 것 | 원 명세 | 이유 |
|---|---|---|
| 카드를 쓰지 않음 | binance `markets-table-card` | 회색 사각형이 화면을 답답하게 만듦. spacex 방식으로 대체 |
| 보조 텍스트를 밝힘 | binance `muted` `#707a8a` | 4.1:1로 AA 미달 |
| 작은 글씨용 `#c9cfd8` 추가 | binance 팔레트에 없음 | `#eaecef`와 `#929aa5` 사이가 비어 있었음 |
| 유리 사용 | binance “글래스모피즘을 쓰지 않는다” | 카드를 없애 명도 차로 만들던 깊이가 사라진 자리를 대체 |
| `body-3` 14/500, `caption` 13/600 | toss 13/400, 12/500 | 다크에서 안 읽힘 |
| 다크 팔레트 전반 | toss는 라이트 전용 | 토스 명세가 다크 alias 미공개라고 명시 |
| 하락 `#c81e33` | toss `red-500` `#f03848` | 흰 배경에서 3.9:1 미달 |
| 이모지 일부 유지 | toss “카피에 inline 이모지 금지” | 🦜 껄무새가 브랜드 자체 |

---

## 8. 구현

- **토큰**: `frontend/src/index.css` 의 CSS 변수. `.dark` 클래스가 같은 변수를 재정의한다.
- **매핑**: `frontend/tailwind.config.js` 가 변수를 Tailwind 색으로 노출한다.
  컴포넌트는 `text-slate-500` 같은 유틸리티를 그대로 쓰고 테마는 자동으로 따라간다.
- **역할별 단계** — `50/100/200` 면, `300/400` 테두리, `500/600` 채움, `700/800` 면 위 글자.
- **브랜드**: `bg-brand` / `text-brand-ink` / `hover:bg-brand-hover`.
- **숫자**: `.num` 유틸리티 (JetBrains Mono + `tabular-nums`).
